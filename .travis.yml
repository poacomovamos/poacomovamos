before_script:
  - npm install karma@~0.9
  - npm install karma-jasmine
language: ruby
rvm:
  - "1.9.3"

  # uncomment this line if your project needs to run something other than `rake`:
script: rake test 
